# Example environment variables for local development
# Do NOT commit real secrets to git. Copy this file to .env and fill values.

# Basic auth for site-server (optional)
PDF_USERNAME=your_username_here
PDF_PASSWORD=your_password_here

# Puppeteer executable path (if using system Chromium)
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# Ports (Render sets PORT automatically)
# PORT=3000
